const Input = () => {
  return (
    <form
      action=""
      className="w-full h-9 bg-zinc-800 flex items-center px-2 rounded"
    >
      <input
        type="text"
        placeholder="Search"
        className="h-full w-full bg-transparent outline-none"
      />
    </form>
  );
};

export default Input;
